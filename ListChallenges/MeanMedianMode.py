
import statistics as st

lst = [6, 8, 4, 9, 3, 10, 8, 11, 5, 7, 8, 4, 2]

mean = st.mean(lst)

median = st.median(lst)

mode = st.mode(lst)

print('Mean:', mean)
print('Median:', median)
print('Mode:', mode)
